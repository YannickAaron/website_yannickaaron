# yannickaaron.com

Terminal-style portfolio. Next.js 16 · Tailwind 4 · no other deps.

```sh
npm install
npm run dev
```

All content lives in `lib/profile.ts`. Commands in `components/commands.tsx`.
